{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "required": ["effectDisplayName", "configurableProperties"],
    "properties": {
        "effectDisplayName": {
            "type": "string",
            "description": "The display name shown in the UI for this effect."
        },
        "disabled": {
            "type": "boolean",
            "default": false,
            "description": "Whether this effect is entirely disabled or not."
        },
        "cssVarPrefix": {
            "type": "string",
            "description": "The prefix for the CSS variables that correspond to the configurable properties in this effect's JSON file."
        },
        "configurableProperties": {
            "type": "array",
            "items": {
                "type": "object",
                "required": ["name", "type", "default", "description"],
                "properties": {
                    "name": {
                        "type": "string",
                        "description": "The name of the configurable property which will be exposed to the user to configure in VSCode as well as exposed to TypeScript."
                    },
                    "type": {
                        "type": "string",
                        "description": "The data type of the configurable property.",
                        "enum": ["boolean", "string", "number"]
                    },
                    "default": {},
                    "description": {
                        "type": "string",
                        "description": "The description of the configurable property which will be shown in the UI to the user as an explanation for the property."
                    },
                    "markdownDescription": {
                        "type": "string",
                        "description": "Equivalent to the description property, but allowing for markdown formatting as well."
                    },
                    "enum": {
                        "type": "array",
                        "items": { "type": "string" },
                        "description": "An array of possible values for the configurable property."
                    },
                    "step": {
                        "type": "number",
                        "description": "The step size for the configurable property in the VS Bloom Settings Editor. Note - this does not restrict users from inputting smaller steps."
                    },
                    "cssUnit": {
                        "type": "string",
                        "description": "A CSS unit string that will be appended to the CSS variable name for the configurable property when it is automatically pulled from the user's VS Code settings and forwarded into a CSS variable on the Electron Renderer's DOM :root element."
                    },
                    "isColor": {
                        "type": "boolean",
                        "description": "Whether the configurable property is a color or not. If true, the VS Bloom Settings Editor will automatically render an interactive color picker for the property."
                    }
                },
                "additionalProperties": false
            }
        }
    },
    "additionalProperties": false
}