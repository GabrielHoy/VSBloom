{
	"version": "0.2.0",
	"configurations": [
		{
			"name": "Run VSBloom",
			"type": "extensionHost",
			"request": "launch",
			"args": [
				"--extensionDevelopmentPath=${workspaceFolder}"
			],
			"outFiles": [
				"${workspaceFolder}/build/**/*.js"
			],
			"preLaunchTask": "${defaultBuildTask}"
		}
	]
}
