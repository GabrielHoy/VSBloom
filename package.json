{
	"preview": true,
	"name": "vsbloom",
	"displayName": "VS: Bloom",
	"description": "Overhauls VSCode's look and feel, adding a variety of animations and 'eye candy' to the UI.",
	"author": {
		"name": "Tampered Reality"
	},
	"version": "1.0.0",
	"publisher": "tamperedreality",
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "https://github.com/GabrielHoy/VSBloom"
	},
	"icon": "logo.png",
	"engines": {
		"vscode": "^1.105.1"
	},
	"categories": [
		"Themes"
	],
	"extensionKind": [
		"ui"
	],
	"main": "./build/VSBloom.js",
	"activationEvents": [
		"*"
	],
	"contributes": {
		"commands": [
			{
				"command": "vsbloom.enable",
				"title": "VSBloom: Enable and Patch Electron Client"
			},
			{
				"command": "vsbloom.retryPatch",
				"title": "VSBloom: Retry Electron Client Patching"
			},
			{
				"command": "vsbloom.disable",
				"title": "VSBloom: Disable and Un-Patch Electron Client"
			}
		],
		"configuration": [
			{
				"title": "Electron Patcher",
				"order": 1,
				"properties": {
					"vsbloom.patcher.appProductFile": {
						"order": 1,
						"type": "string",
						"default": "auto",
						"description": "The path to the 'product.json' file associated with the VSCode application itself; setting this to 'auto' will attempt to locate the appropriate path for this file automatically - it's usually located somewhere around %localappdata%\\Programs\\Microsoft VS Code\\resources\\app\\product.json",
						"markdownDescription": "The path to the `product.json` file associated with the VSCode application itself; setting this to '**auto**' will attempt to locate the appropriate path for this file automatically - it's usually located somewhere around `%localappdata%\\Programs\\Microsoft VS Code\\resources\\app\\product.json`"
					},
					"vsbloom.patcher.suppressCorruptionWarning": {
						"order": 2,
						"type": "boolean",
						"default": true,
						"description": "Whether or not to try and suppress the 'client corrupted' warning message that may appear as a result of VSBloom modifying files related to the Electron renderer.",
						"markdownDescription": "Whether or not to try and suppress the 'client corrupted' warning message that may appear as a result of VSBloom modifying files related to the Electron renderer."
					}
				}
			},
			{
				"title": "Window Effects",
				"order": 2,
				"properties": {
					"vsbloom.windowFocus.enabled": {
						"order": 3,
						"type": "boolean",
						"default": true,
						"markdownDescription": "Enable or disable effects surrounding the window becoming unfocused *(and later refocused)*",
						"description": "Enable or disable window-focus based effects"
					},
					"vsbloom.windowFocus.longTermUnfocusThreshold": {
						"order": 4,
						"type": "number",
						"default": 30,
						"markdownDescription": "A threshold - in **seconds** - for how long the window must remain unfocused: After this time the 'short-term' unfocus effect transitions into a heavier 'long-term' effect",
						"description": "A threshold - in seconds - for how long the window must remain unfocused: After this time the 'short-term' unfocus effect transitions into a heavier 'long-term' effect"
					}
				}
			},
			{
				"title": "Code Editor Effects",
				"order": 3,
				"properties": {
					"vsbloom.cursorTrail.enabled": {
						"order": 5,
						"type": "boolean",
						"default": true,
						"description": "Enable or disable the cursor trail effect"
					},
					"vsbloom.cursorTrail.duration": {
						"order": 6,
						"type": "number",
						"default": 750,
						"description": "The lifetime - in milliseconds - of the trail that follows behind the cursor as it moves.",
						"markdownDescription": "The lifetime - in **milliseconds** - of the trail that follows behind the cursor as it moves."
					}
				}
			}
		]
	},
	"scripts": {
		"vscode:prepublish": "npm run package",
		"compile": "node esbuild.js",
		"watch": "node esbuild.js --watch",
		"package": "node esbuild.js --production",
		"compile-tests": "tsc -p . --outDir build",
		"pretest": "npm run compile-tests && npm run compile && npm run lint",
		"lint": "eslint src",
		"test": "vscode-test"
	},
	"devDependencies": {
		"@types/mocha": "^10.0.10",
		"@types/node": "22.x",
		"@types/vscode": "^1.105.1",
		"@types/ws": "^8.18.1",
		"@vscode/sudo-prompt": "^9.3.2",
		"@vscode/test-cli": "^0.0.12",
		"@vscode/test-electron": "^2.5.2",
		"esbuild": "^0.27.2",
		"eslint": "^9.39.2",
		"terser": "^5.46.0",
		"typescript": "^5.9.3",
		"typescript-eslint": "^8.52.0"
	},
	"dependencies": {
		"async-mutex": "^0.5.0",
		"ws": "^8.19.0"
	}
}
