/**
 * Bloom: The VSBloom Client's 'All-Purpose' Utility Container
 */

/// <reference lib="dom" />

import * as dom from './BloomDOM'; 
import * as configs from './BloomConfigs';

const bloom = {
    dom,
    configs,
};

export default bloom;