//this tsconfig corresponds to svelte files within the webview
{
	"compilerOptions": {
		"module": "esnext",
		"moduleResolution": "bundler",
		"target": "ESNext",
		/**
		Svelte Preprocess cannot figure out whether we're importing a value/type, so tell TypeScript
		to enforce using `import type` instead of `import` for types
		*/
		"verbatimModuleSyntax": true,
		/**
		To have warnings/errors of the Svelte compiler at the correct position,
		enable source maps by default.
		*/
		"sourceMap": true,

		"resolveJsonModule": true,

		"strict": true,
		"esModuleInterop": true,
		"skipLibCheck": true,
		"noEmit": true,
		"paths": {
			"$webview-svelte-lib": ["./Libraries"],
			"$webview-svelte-lib/*": ["./Libraries/*"]
		}
	},
  
	"include": ["**/*", "SvelteMounter.ts.ts", "../Extension/WebviewMenuPanel.ts"],
	"exclude": ["node_modules/*", "__sapper__/*", "public/*"]
}